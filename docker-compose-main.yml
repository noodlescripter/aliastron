version: '3.8'

services:
  arch-btw-web:
    image: nginx:alpine
    container_name: arch-btw-web
    ports:
      - "5000:5000"
    volumes:
      - ./index.html:/usr/share/nginx/html/arch-btw.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    networks:
      - aliastron-network

networks:
  aliastron-network:
    driver: bridge
